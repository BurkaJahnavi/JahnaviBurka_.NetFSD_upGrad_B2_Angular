Win. forms App			WPF App
Code + Graphic			Code Graphic
    CPU				CPU	GPU (Graphic Card)


****VES (Virtual Execution System of CLR)
1.C#(.cs)=====>Roslyn (.cs) (build)==>

2.MSIL(Microsoft Intermediate Language):IL (meta data)-PE (Portable executable)-.exe,.dll(dynamic link library)(PE)==>

3.CLS(Common Language Specification)=>(PE) a.Common Rules across all languages
					C#- Array starts index 0
					Vb.Net-Array starts index 0
					
	b. it ensures compatibility of BCL libraries with language

4.CTS (Common Type System): it gives type safety and language interoperability.

	
C#(Rosly)	vb.net(VBC)		CLR(CTS)
int		Integer			Int32 (System.Int32)
short		Short			Int16 (System.Int16)
long		Long			Int64 (System.Int64)
string		String			string (System.String)

int a;		Dim a as Integer	Int32 (System.Int32)

5.JIT (Just-In-Time):converts IL(PE) code into native machine code (Binary Machine Code).


**************************************************************************

1.GAC (Global Assembly Cache):It is registry where all BCL are registered.

2.BCL:(Base Class Library)/FCL (Framework Class Library)/(Core Library)

=>It is collection of .dll(dynamic link library) files

3. .DLL: (dynamic link library):it is logical collection/grouping  of namespaces.

4.namespace: Nested Namespaces and logical grouping of | Classes | Interfaces | Structures | Delegates | Enum

mscorlib.dll(Shared assembly)-->System(root namespace)===>Text(namespace)===>StringBuilder(class)

using System.Text;

Main()
{
	StringBuilder string1=new StringBuilder();
}









